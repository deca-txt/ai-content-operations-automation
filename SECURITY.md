# Security

This repository contains a sanitized product case, not a deployable production environment.

If you discover that a secret, token, private endpoint or operational identifier was accidentally committed, do not repost it in an issue.

Remove the secret from the repository history and rotate/revoke it in the originating service.
